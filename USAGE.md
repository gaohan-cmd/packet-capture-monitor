# Packet Capture Monitor 使用文档

本文档面向使用者，说明如何打开看板、配置浏览器代理、安装 HTTPS 解密证书，以及如何查看抓包数据。

当前公网服务信息：

- 看板地址：`https://capture.example.com`
- 代理主机：`203.0.113.10` 或 `capture.example.com`
- 代理端口：`8081`
- 代理认证：使用管理员分配给你的 `proxy_username` 和 `proxy_password`

不要把代理账号密码公开分享。代理会解密并展示经过它的 HTTP(S) 流量，只能用于你拥有或明确授权调试的网站。

## 登录看板

打开：

```text
https://capture.example.com
```

输入管理员分配给你的看板账号。多用户部署时，账号来自 `DASHBOARD_USERS`：

```env
username=看板用户名
password=看板密码
```

登录后只能看到你自己的请求列表、请求详情、目标配置和清空操作。其他用户的数据不会显示在你的看板中。

## 设置采集目标

每个用户都有自己的默认采集目标。示例：

```text
https://ikuuu.win
```

也可以在看板顶部修改目标地址，例如：

```text
https://example.com
```

采集规则：

- 只展示目标地址匹配的请求。
- 你的账号配置了 `include_subdomains=true` 时，会同时采集子域名。
- 目标地址包含路径时，只采集该路径及其子路径。

修改目标后，代理会定时从看板同步配置，通常几秒内生效。

## 配置浏览器代理

把浏览器或系统代理设置为：

```text
HTTP Proxy:  203.0.113.10
HTTP Port:   8081
HTTPS Proxy: 203.0.113.10
HTTPS Port:  8081
Username:    管理员分配给你的 proxy_username
Password:    管理员分配给你的 proxy_password
```

也可以把代理主机写成域名：

```text
capture.example.com
```

推荐只给用于调试的浏览器配置代理，不要直接给整台电脑长期启用全局代理。

## 安装 HTTPS 解密证书

首次抓 HTTPS 请求时，必须安装并信任 mitmproxy CA 证书。

1. 先完成浏览器代理配置。
2. 在已经走代理的浏览器里打开：

```text
http://mitm.it
```

3. 下载当前系统对应的证书。
4. 安装并信任该证书。
5. 重新访问目标网站。

macOS：

- 下载证书后打开“钥匙串访问”。
- 把证书加入“系统”或“登录”钥匙串。
- 打开证书详情，在“信任”中设置为“始终信任”。
- 重新启动浏览器。

Windows：

- 下载证书后双击安装。
- 选择“本地计算机”或“当前用户”。
- 放入“受信任的根证书颁发机构”。
- 重新启动浏览器。

iOS：

- 用 Safari 打开 `http://mitm.it` 下载 iOS 证书描述文件。
- 到“设置”中安装描述文件。
- 到“设置 > 通用 > 关于本机 > 证书信任设置”中启用完全信任。

Android：

- 用浏览器打开 `http://mitm.it` 下载 Android 证书。
- 按系统提示安装为用户证书。
- 注意：部分 Android 应用默认不信任用户证书，只能抓浏览器或允许用户证书的应用流量。

## 验证代理是否生效

未配置代理认证时访问代理，应该被拒绝：

```text
407 Proxy Authentication Required
```

配置正确后，访问目标网站，看板里应出现请求记录。

如果访问 `http://mitm.it` 或 `http://example.com` 时看到 DNSPod、运营商或云厂商拦截页，通常是网络环境对这些域名做了重定向；这不一定代表代理失败。以看板是否出现目标网站请求、代理是否要求认证为准。

## 查看抓包数据

看板主要区域：

- 请求列表：展示方法、状态码、路径、耗时、响应大小、内容类型。
- 详情视图：展示请求概览、请求头、响应头、请求体、响应体。
- 响应预览：JSON 会格式化展示，文本/HTML 会以文本预览，图片会显示预览，二进制内容会显示摘要。

敏感头处理：

- `Authorization`
- `Cookie`
- `Set-Cookie`

这些头默认会被隐藏，需要时可以手动显示。显示前请确认环境安全，避免泄露账号、Token 或 Session。

## 常见问题

### 看板能打开，但没有任何请求

检查：

- 浏览器是否配置了 `HTTP` 和 `HTTPS` 代理。
- 代理主机是否是 `203.0.113.10` 或 `capture.example.com`。
- 代理端口是否是 `8081`。
- 是否使用了你自己的 `proxy_username` 和 `proxy_password`。
- 是否安装并信任了 mitmproxy CA 证书。
- 看板顶部目标地址是否和正在访问的网站一致。

### 只能看到 HTTP，看不到 HTTPS 内容

通常是证书没有安装或没有被信任。重新访问 `http://mitm.it` 下载证书，并确认系统已经信任该证书。

### 浏览器提示连接不安全

如果目标网站是 HTTPS，且浏览器没有信任 mitmproxy CA，会出现证书错误。安装并信任证书后再试。

### 代理认证一直失败

确认使用的是管理员分配给你的代理账号，不是看板登录账号。浏览器代理设置里用户名填 `proxy_username`，密码填 `proxy_password`。

如果密码包含特殊字符，某些浏览器或代理插件可能需要重新输入，避免复制时带入空格。

### 不想继续抓包

关闭浏览器或系统代理即可。为了安全，调试结束后建议：

- 关闭代理配置。
- 清空看板中的敏感记录，或按运维流程清理数据库。
- 不再需要时删除本机安装的 mitmproxy CA 证书。


公网部署说明见 [DEPLOYMENT.md](DEPLOYMENT.md)。
